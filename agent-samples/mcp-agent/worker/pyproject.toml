[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcp-agent-worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "xr-ai-agent",
    "numpy>=1.24",
    "httpx>=0.27",
    "pyyaml>=6.0",
]

[tool.uv.sources]
xr-ai-agent = { path = "../../../agent-sdk", editable = true }

[project.scripts]
mcp_agent_worker = "mcp_agent_worker.__main__:run"

[tool.hatch.build.targets.wheel]
packages = ["mcp_agent_worker"]
